{
  "name": "api",
  "version": "1.0.0",
  "description": "meli challenge api",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "cd .. & husky install ./api",
    "format": "prettier --write ."
  },
  "keywords": [
    "api",
    "express",
    "meli",
    "challenge"
  ],
  "author": "jafidiesel",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.1.1",
    "husky": "^9.0.11",
    "prettier": "^3.2.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    },
    "lint-staged": {
      "*.js": [
        "eslint — fix",
        "prettier — write",
        "git add"
      ]
    }
  }
}
